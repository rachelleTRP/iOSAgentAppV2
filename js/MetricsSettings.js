'use strict';

var React = require('react-native');
var {
    AppRegistry,
    StyleSheet,
    Text,
    View,
    NavigatorIOS,
    TabBarIOS,
    ActivityIndicatorIOS,
    ScrollView
} = React;

var store = require('react-native-simple-store');
// TODO: use this asyncStorage store to store metrics dashboard display preferences
var Styles = require('./Styles.js');

var MetricsSettings = React.createClass({




    render: function() {
        return (

        );
    }
});


module.exports = MetricsSettings; 